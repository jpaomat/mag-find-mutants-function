import { httpStatus } from '../../src/utils/httpStatus';

describe('httpStatus', () => {
    test('should be created', async () => {
        // httpStatus
        // expect(service).toBeTruthy();
    });

});
